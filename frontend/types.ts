export interface Drug {
  id: string;
  name: string;
  price: string;
  drug_code: string;
  drugName: string;
  customer_condition: string;
  id_check: number;
  store: string;
  postcode: string;
  availalble_stock: number;
  expiry_date: string;
  availability: number;
}
